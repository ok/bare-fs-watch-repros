{
  "name": "bare-fs-repro-null-filename",
  "private": true,
  "version": "0.0.0",
  "description": "bare-fs crashes on the NULL filename libuv passes on a Windows buffer overflow",
  "dependencies": {
    "bare-fs": "^4.8.1",
    "bare-os": "^3.9.3",
    "bare-path": "^3.1.2",
    "bare-runtime": "^1.33.4"
  }
}
